<nav class="navbar navbar-expand-lg bg-body-tertiary sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{path('app_index')}}">
            <img src="{{asset('assets/img/logo-navbar.png')}}" alt="Logo" width="100" class="animate__animated animate__bounce"
                height="45" class="d-inline-block align-text-top">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" id="home-nav-link" aria-current="page" href="{{path('app_index')}}">{% trans %}ACCUEIL{% endtrans %}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="about-nav-link" href="{{path('app_about')}}">{% trans %}À PROPOS{% endtrans %}</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle header-link-accentuate" href="#" role="button" data-bs-toggle="dropdown"
                        aria-expanded="false">
                        {% trans %}PRESTATIONS DE SERVICES{% endtrans %}
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{path('app_individuals_services')}}">{% trans %}POUR PARTICULIER{% endtrans %}</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="{{path('app_businesses_services')}}">{% trans %}POUR ENTREPRISE{% endtrans %}</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="appointment-nav-link" href="{{path('app_appointment')}}">{% trans %}PRENDRE RENDEZ-VOUS{% endtrans %}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="contact-nav-link" href="{{path('app_contact')}}">{% trans %}CONTACT{% endtrans %}</a>
                </li>
                {% if(app.user) %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('admin')}}">{% trans %}ADMINISTRATION{% endtrans %}</a>
                    </li>
                {% endif %}
                <li class="header-v-separator"></li>
                {% for locale in locales %}
                    {% if locale != app.request.locale %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('app_change_locale', {'lang': locale})}}">
                                <img src="{{asset('assets/icones/'~locale~'.png')}}" alt="{{locale}}" class="langue-icone">
                            </a>
                        </li>
                    {% endif %}
                {% endfor %}
            </ul>
        </div>
    </div>
</nav>